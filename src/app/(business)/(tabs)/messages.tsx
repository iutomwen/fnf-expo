import { View, Text } from "react-native";
import React from "react";
import MessageCenter from "@/app/(common)/message";

const BusinessMessageScreen = () => {
  return <MessageCenter />;
};

export default BusinessMessageScreen;
